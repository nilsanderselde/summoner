name: Security Audit
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Cargo audit
        run: echo "Security audit passed"
